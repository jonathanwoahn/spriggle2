import { Box } from "@mui/material";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Spriggle Administration'
};


export default async function AdminPage() {
  return (
    <Box>
      ADMIN PAGE
    </Box>
  );
}